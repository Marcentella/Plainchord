# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
*.tsbuildinfo
next-env.d.ts

# debug
npm-debug.log*

# env / secrets
.env*
!.env.example

# vercel
.vercel

# project docs (not for the public repo)
FEATURES.md
CONTEXT.md
PONYTAIL-DEBT.md

# local tooling/config (not for the public repo — same category as the docs above)
/.agents/
/plans/
/skills-lock.json
.claude/skills/animate/
.claude/skills/improve-animations/

# user-dropped Guitar Pro files used for local testing — never committed,
# per the no-hosting-rule skill (PlainChord doesn't host copyrighted content)
public/*.gp3
public/*.gp4
public/*.gp5
public/*.gpx
